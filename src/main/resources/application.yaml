spring:
  application:
    name: quizapp

  datasource:
    url: jdbc:postgresql://localhost:5432/questiondb?useSSL=false
    username: postgres
    password: Podman56
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  show-sql: true
